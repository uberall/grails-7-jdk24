name: CI

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 24
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '24'
          cache: 'gradle'

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Gradle Clean
        run: ./gradlew --no-daemon clean

      - name: Set up JDK 17 for CodeNarc
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: 'gradle'

      - name: CodeNarc (runs with JDK 17)
        run: ./gradlew --no-daemon codenarc || ./gradlew --no-daemon codenarcMain codenarcTest

      - name: Restore JDK 24 for build/tests
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '24'
          cache: 'gradle'

      - name: Test
        run: ./gradlew --no-daemon test

      - name: Build
        run: ./gradlew --no-daemon build
